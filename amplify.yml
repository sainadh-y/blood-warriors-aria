version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend || true
        - npm ci
    build:
      commands:
        - cd frontend || true
        - npm run build
  artifacts:
    baseDirectory: frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*